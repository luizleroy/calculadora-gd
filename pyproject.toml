[project]
name = "calculadora-GD"
version = "0.1.0"
description = "Leitura e cálculo de faturas de energia"
requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pypdf"
]